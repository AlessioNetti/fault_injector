load name=meminfo
config name=meminfo producer=localhost instance=localhost/meminfo
start name=meminfo interval=10000000

load name=procstat
config name=procstat producer=localhost instance=localhost/procstat
start name=procstat interval=10000000
